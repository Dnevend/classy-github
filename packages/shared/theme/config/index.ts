import defaultData from './default.json'
import nesData from './nes.json'

export const defaultConfig = defaultData

export const nesConfig = nesData

export default {
    defaultConfig,
    nesConfig
}